# Quantization Blog Rewrite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite the quantization article into the approved six-section deep research explainer, with stable image embeds and a tighter causal narrative.

**Architecture:** Work in place on the existing article file. First lock the final section skeleton and image slots, then rewrite the prose section-by-section in narrative order, preserving the research substance while removing grandiose language, duplicated survey material, and broken embedded image data.

**Tech Stack:** Markdown, Obsidian wikilinks, ripgrep, Python 3, git

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md` | Modify | Final article text, headings, image embeds, cleaned narrative flow |
| `misc/quantization_memory_wall.png` | Consume | Intro image slot generated by the figures plan |
| `misc/quantization_intuition.png` | Consume | Basic intuition image slot generated by the figures plan |
| `misc/quantization_formula.png` | Consume | Formula image slot generated by the figures plan |
| `misc/quantization_outlier_distortion.png` | Consume | Outlier image slot generated by the figures plan |
| `docs/superpowers/specs/2026-04-17-quantization-blog-design.md` | Reference | Approved structure and editorial rules |
| `docs/superpowers/specs/2026-04-17-quantization-figures-design.md` | Reference | Approved figure set and target filenames |

---

### Task 1: Lock the final article skeleton

**Files:**
- Modify: `Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md`

- [ ] **Step 1: Replace the current top-level structure with the approved section map**

```markdown
TL;DR Quantization makes large language models practical to run by compressing their numerical representation, but every bit you remove creates trade-offs in accuracy, robustness, and behavior.

## 1. Introduction: Why Quantization Exists
### 1.1 The memory wall
### 1.2 Why scaling LLMs makes this unavoidable
### 1.3 What this article will explain

![[quantization_memory_wall.png]]

## 2. The Basic Intuition Behind Quantization
### 2.1 From high precision to low-bit representation

![[quantization_intuition.png]]

### 2.2 The quantization formula, reconstruction, and error

![[quantization_formula.png]]

### 2.3 What is actually lost when precision goes down

## 3. Why Naive Quantization Fails in LLMs
### 3.1 Why simple rounding breaks large models
### 3.2 Outliers, step-size inflation, and massive activations
### 3.3 Why this problem gets worse at scale

![[quantization_outlier_distortion.png]]

## 4. How Quantization Is Actually Applied Today
### 4.1 Post-Training Quantization vs Quantization-Aware Training
### 4.2 What modern methods try to preserve
### 4.3 Main methods and formats in practice
### 4.4 When GGUF, AWQ, GPTQ, EXL2, or QAT make sense

## 5. The Hidden Costs of Quantization
### 5.1 Capability degradation: reasoning, context, and multimodal reliability
### 5.2 Silent failures in agents and real-world behavior
### 5.3 Security, bias, and alignment distortions

## 6. Conclusion: Quantization as a Core Design Layer
### 6.1 What quantization really gives us
### 6.2 What it can quietly take away
### 6.3 Where research is going next
```

- [ ] **Step 2: Remove obsolete top-level sections from the old draft**

Run: `rg -n "^## " 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected: only `## 1` through `## 6` appear.

- [ ] **Step 3: Verify that the future image filenames are the only quantization figure embeds**

Run: `rg -n "scene1_precision_loss|scene2_memory_wall|scene3_quant_formula|quantization_" 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected: only `quantization_memory_wall`, `quantization_intuition`, `quantization_formula`, and `quantization_outlier_distortion` remain.

- [ ] **Step 4: Commit the structural scaffold**

```bash
git add -- 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'
git commit -m "docs: scaffold quantization article rewrite"
```

---

### Task 2: Rewrite the introduction and basic intuition section

**Files:**
- Modify: `Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md`

- [ ] **Step 1: Rewrite Section 1 with a concrete problem-first framing**

Use this content contract directly in the article:

```markdown
## 1. Introduction: Why Quantization Exists

### 1.1 The memory wall

Open by tying LLM scaling to memory bandwidth and VRAM limits, not to abstract optimization language. State plainly that model size, not just compute, becomes a deployment constraint.

### 1.2 Why scaling LLMs makes this unavoidable

Use one concrete example such as a 70B model in FP16 or BF16 to show why full-precision inference becomes expensive or inaccessible. Keep the quantitative claim simple and readable.

### 1.3 What this article will explain

Close the introduction by promising three things in this order: how quantization works, why naive quantization fails, and what real costs appear when a model is compressed.
```

- [ ] **Step 2: Rewrite Section 2 as the pedagogical foundation**

Use this content contract directly in the article:

```markdown
## 2. The Basic Intuition Behind Quantization

### 2.1 From high precision to low-bit representation

Explain quantization as mapping many nearby continuous values into a smaller discrete set. Keep the wording plain and visual.

### 2.2 The quantization formula, reconstruction, and error

Walk through `W`, `Δ`, `Z`, `Round + Clip`, and `W_q` in the same order the figure uses. Then show dequantization as approximate reconstruction rather than exact recovery.

### 2.3 What is actually lost when precision goes down

Link lower bit-width to larger step size, larger reconstruction error, and loss of fine-grained distinction between nearby values.
```

- [ ] **Step 3: Make the prose explicitly reference the formula colors**

Add text in the style of:

```markdown
The blue `W` is the original high-precision value. The green `Δ` controls the size of each quantization step. The amber `Z` shifts the grid, and the red `Round + Clip` stage forces the result into the allowed discrete range.
```

Keep the exact wording natural, but preserve the semantic order.

- [ ] **Step 4: Validate section structure**

Run: `rg -n "^## 1|^### 1\\.|^## 2|^### 2\\." 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected: Section `1` has exactly three `###` headings and Section `2` has exactly three `###` headings.

- [ ] **Step 5: Commit the first rewritten block**

```bash
git add -- 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'
git commit -m "docs: rewrite quantization intro and basics"
```

---

### Task 3: Rewrite the failure bridge section

**Files:**
- Modify: `Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md`

- [ ] **Step 1: Rewrite Section 3 around causality, not paper chronology**

Use this content contract directly in the article:

```markdown
## 3. Why Naive Quantization Fails in LLMs

### 3.1 Why simple rounding breaks large models

Start from the tempting naive idea: if weights are just numbers, why not round them directly? Then explain why that intuition fails in practice.

### 3.2 Outliers, step-size inflation, and massive activations

Show the mechanism: one extreme outlier forces a larger `Δ`, and that coarser scale collapses the useful resolution of the ordinary values.

### 3.3 Why this problem gets worse at scale

Connect the failure to large-model behavior: emergent outliers, token-level activation pathologies, and why this becomes a structural issue in large transformers.
```

- [ ] **Step 2: Remove the inline base64 image and replace it with the approved outlier figure**

Run: `rg -n "data:image" 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected before edit: one hit.

After replacement, run the same command again.
Expected after edit: no output.

- [ ] **Step 3: Add a transition sentence from Section 3 into Section 4**

Use a sentence with this meaning:

```markdown
Once naive quantization is understood as a failure of scale management and error control, modern quantization methods stop looking like arbitrary acronyms and start looking like targeted responses.
```

Keep the sentence concise, but preserve that logic.

- [ ] **Step 4: Validate that the outlier figure is embedded once**

Run: `rg -n "quantization_outlier_distortion" 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected: exactly one hit in Section `3`.

- [ ] **Step 5: Commit the failure bridge rewrite**

```bash
git add -- 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'
git commit -m "docs: rewrite quantization failure bridge"
```

---

### Task 4: Rewrite the methods section around problem-response logic

**Files:**
- Modify: `Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md`

- [ ] **Step 1: Rewrite Section 4 using PTQ vs QAT as the first organizing split**

Use this content contract directly in the article:

```markdown
## 4. How Quantization Is Actually Applied Today

### 4.1 Post-Training Quantization vs Quantization-Aware Training

Define the two families cleanly and contrast speed, cost, and robustness.

### 4.2 What modern methods try to preserve

Explain that different methods protect different things: salient weights, local scales, throughput, bitrate flexibility, or quality retention.

### 4.3 Main methods and formats in practice

Introduce GGUF, AWQ, GPTQ, EXL2, and QAT only after the reader understands the problem each one is solving.

### 4.4 When GGUF, AWQ, GPTQ, EXL2, or QAT make sense

Close with practical positioning rather than benchmark dumping.
```

- [ ] **Step 2: Compress method comparison into one high-signal table**

Insert a single Markdown table shaped like this:

```markdown
| Method / Format | Main idea | Strength | Best fit |
|---|---|---|---|
| GGUF | block-wise low-bit format tuned for broad local inference | portability and practical defaults | CPUs, Apple Silicon, heterogeneous local setups |
| AWQ | protect activation-salient weights during quantization | strong quality retention at low bit-width | GPU inference where quality matters |
| GPTQ | second-order post-training weight quantization | fast inference with good compression | throughput-oriented GPU serving |
| EXL2 | mixed bitrate quantization tuned to fit VRAM targets | flexible memory / speed trade-off | multi-GPU or VRAM-constrained ExLlama setups |
| QAT | train or fine-tune with quantization in the loop | best robustness at very low precision | cases where retraining cost is acceptable |
```

Fill every cell with a short, concrete phrase. Do not add a second comparison table unless the first one demonstrably fails.

- [ ] **Step 3: Keep deployment detail as context only**

Run: `rg -n "Apple|Blackwell|NPU|ROCm|CUDA|M4|NVFP4" 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected after edit: any surviving hardware terms appear only inside short contextual sentences, not as a standalone section.

- [ ] **Step 4: Validate the method section shape**

Run: `rg -n "^## 4|^### 4\\." 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected: one `## 4` heading and four `### 4.x` headings.

- [ ] **Step 5: Commit the methods rewrite**

```bash
git add -- 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'
git commit -m "docs: rewrite quantization methods section"
```

---

### Task 5: Rewrite the hidden costs and conclusion

**Files:**
- Modify: `Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md`

- [ ] **Step 1: Merge the current risk material into the approved Section 5**

Use this content contract directly in the article:

```markdown
## 5. The Hidden Costs of Quantization

### 5.1 Capability degradation: reasoning, context, and multimodal reliability

Group quality degradation together instead of splitting it across many risk sections.

### 5.2 Silent failures in agents and real-world behavior

Cover agent drift, tool-calling brittleness, and hard-to-detect operational failure.

### 5.3 Security, bias, and alignment distortions

Discuss safety, jailbreak robustness, and bias carefully, as degradations that may appear under compression, not as guaranteed outcomes.
```

- [ ] **Step 2: Rewrite the conclusion as analysis, not rhetoric**

Use this content contract directly in the article:

```markdown
## 6. Conclusion: Quantization as a Core Design Layer

### 6.1 What quantization really gives us
### 6.2 What it can quietly take away
### 6.3 Where research is going next
```

The conclusion should summarize the trade-off, not introduce a new manifesto or geopolitical argument.

- [ ] **Step 3: Remove tone that weakens technical credibility**

Run: `rg -n "Cambrian explosion|moral equity|axiological|colossal syntactic constellations|absolute backbone" 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected after edit: no output.

- [ ] **Step 4: Validate the final section structure**

Run: `rg -n "^## 5|^### 5\\.|^## 6|^### 6\\." 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'`
Expected: Section `5` has three `###` headings and Section `6` has three `###` headings.

- [ ] **Step 5: Commit the closing sections**

```bash
git add -- 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'
git commit -m "docs: rewrite quantization risks and conclusion"
```

---

### Task 6: Final cleanup and article verification

**Files:**
- Modify: `Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md`

- [ ] **Step 1: Run a structural sanity check**

```bash
python3 - <<'PY'
from pathlib import Path
import re
p = Path("Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md")
text = p.read_text()
print("h2", len(re.findall(r"^## ", text, re.M)))
print("h3", len(re.findall(r"^### ", text, re.M)))
print("data_uri", "data:image" in text)
print("embeds", re.findall(r"!\\[\\[(.*?)\\]\\]", text))
PY
```

Expected:
- `h2 6`
- `h3 19`
- `data_uri False`
- embeds list contains the four `quantization_*.png` images and no old `scene*.png` names

- [ ] **Step 2: Run a style sanity scan**

```bash
rg -n "lorem ipsum|Cambrian explosion|moral equity|axiological|scene1_precision_loss|scene2_memory_wall|scene3_quant_formula|data:image" \
  'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'
```

Expected: no output.

- [ ] **Step 3: Read the article top to bottom once and tighten transitions**

Focus on whether each section earns the next one:
- introduction -> intuition,
- intuition -> failure,
- failure -> methods,
- methods -> costs,
- costs -> conclusion.

- [ ] **Step 4: Commit the cleaned final rewrite**

```bash
git add -- 'Blog-research/Quantizating LLMs, How it works and what you should be concerned about.md'
git commit -m "docs: finalize quantization article rewrite"
```
